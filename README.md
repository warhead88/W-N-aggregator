<div align="center">
  <img src="https://img.shields.io/badge/Telegram-Bot-blue?style=for-the-badge&logo=telegram">
  <h1>🌩️ W-N-Aggregator (TeleNetAggregator)</h1>
  <p><b>Мощный Telegram-бот агрегатор новостей и погоды, написанный на Python.</b></p>
  
  <p>
    <img src="https://img.shields.io/badge/Python-3.12-blue?style=flat-square&logo=python" alt="Python Version">
    <img src="https://img.shields.io/badge/Aiogram-3.x-blueviolet?style=flat-square" alt="Aiogram">
    <img src="https://img.shields.io/badge/SQLAlchemy-2.0-red?style=flat-square&logo=sqlalchemy" alt="SQLAlchemy">
    <img src="https://img.shields.io/badge/Docker-Ready-2496ED?style=flat-square&logo=docker" alt="DockerReady">
    <img src="https://img.shields.io/github/actions/workflow/status/warhead88/W-N-aggregator/tests.yml?style=flat-square&logo=githubactions" alt="Tests">
  </p>
</div>

---

## 🚀 Особенности

* 🌤 **Актуальная погода и прогноз:** Получайте текущую сводку и точный прогноз (до 3-х дней) благодаря интеграции с [OpenWeatherMap API](https://openweathermap.org/).
* 📰 **Персонализированные новости:** Настройте свои ключевые слова и получайте свежие сводки с помощью [GNews API](https://gnews.io/).
* ⏰ **Ежедневные дайджесты:** Автоматическая кроулер-рассылка по гибкому расписанию с учетом индивидуального часового пояса пользователя.
* 🛡 **Современная архитектура:** Асинхронное взаимодействие, Dependency Injection через middleware и чистая структура по стандартам индустрии.
* 🐳 **Простота развертывания:** Все сервисы, включая PostgreSQL, поднимаются одной командой через Docker Compose.

---

## 🛠 Технологический стек

* **Core:** Python 3.12, `aiogram 3.x` (Асинхронный фреймворк для Telegram).
* **База данных:** PostgreSQL 16, `asyncpg` (Асинхронный драйвер БД).
* **ORM и Миграции:** `SQLAlchemy 2.0+`, `Alembic`.
* **Фоновые задачи:** `APScheduler` (Асинхронный планировщик).
* **Управление зависимостями:** `pyproject.toml` (современный стандарт).
* **Тестирование и CI:** `pytest`, `pytest-asyncio`, `pytest-mock`, GitHub Actions.
* **Инфраструктура:** Docker, Docker Compose.

---

## 🏗 Архитектура проекта

Проект спроектирован с упором на расширяемость и чистый код, а все исходники изолированы в директории `src/`:

```text
W-N-aggregator/
├── src/                      # Исходный код приложения
│   ├── bot.py                # Точка входа, регистрация роутеров и мидлварей
│   ├── db/                   # Модели SQLAlchemy 2.0 (tables.py), конфиг БД
│   ├── handlers/             # Обработчики команд Telegram (start, menu, погода, новости)
│   ├── keyboards/            # Маршрутизация и Inline-клавиатуры
│   ├── middlewares/          # Middleware (например, внедрение DB Session)
│   └── services/             # Бизнес-логика: интеграция API (news, weather), планировщик
│
├── tests/                    # Unit-тесты (pytest)
│   ├── conftest.py           # Мок-фикстуры (AsyncMock, MagicMock для сессий БД и aiogram)
│   ├── test_handlers/        # Спеки тестирования пользовательских хендлеров и FSM
│   └── ...
│
├── migrations/               # Система миграций базы данных (Alembic)
├── pyproject.toml            # Декларативная конфигурация пакетов
├── docker-compose.yml        # Оркестрация контейнеров (Бот + Postgres)
└── Dockerfile                # Сборка образа бота
```

* **Архитектурное решение Dependency Injection**: База данных пробрасывается в хендлеры автоматически с помощью кастомного слоя `DbSessionMiddleware`, что избавляет код команд от ручного вызова контекстного менеджера `async with get_async_session()`.

---

## ⚡ Быстрый старт (Docker)

Проект полностью контейнеризирован и готов к моментальному развертыванию. Для запуска вам потребуются только **Docker** и **Docker Compose**.

### 1. Клонирование репозитория
```bash
git clone https://github.com/warhead88/W-N-aggregator.git
cd W-N-aggregator
```

### 2. Конфигурация окружения
Создайте файл `.env` из поставляемого шаблона:
```bash
cp .env.example .env
```
Откройте файл `.env` любым текстовым редактором и вставьте ваши ключи API (Telegram Bot Token, OpenWeather API, GNews API).

### 3. Запуск и развертывание
База данных, миграции (Alembic) и запуск самого бота происходят **автоматически**.
```bash
docker-compose up -d --build
```
> Бот запущен! Посмотреть логи в реальном времени можно командой: `docker-compose logs -f bot`

Остановить контейнеры:
```bash
docker-compose down
```

---

## 🧪 Тестирование

Проект обладает широким покрытием Unit-тестами, проверяющими интеграцию с внешними API, стейт-машины (FSM) в боте и расписание (scheduler). 

В проекте используется `pytest` вместе со вспомогательными библиотеками. HTTP-запросы и база данных изолированы (mocked). Убедитесь, что у вас есть виртуальное окружение и установлены dev-зависимости, затем запустите:

```bash
# Установка dev-зависимостей из pyproject.toml
pip install -e .[dev]

# Запуск всего набора тестов
pytest -v tests/
```

Проект также подключен к системе непрерывной интеграции (CI) **GitHub Actions**, где линтинг (`flake8`) и тесты будут прогоняться автоматически при любых изменениях (Push / Pull Request) в ветку `main`.
